# Argo CD - base values (all stages)
# Override in apps/argocd/<stage>/values.yaml per stage

argo-cd:
  # Server and repo config - tune per stage if needed
  server:
    replicas: 1
  repoServer:
    replicas: 1
  # Uncomment and set for HA / resource limits
  # controller:
  #   replicas: 2
