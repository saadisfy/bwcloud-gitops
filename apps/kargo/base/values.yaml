# Kargo - base values (prod only)
# OCI chart: oci://ghcr.io/akuity/kargo-charts/kargo (dependency vendored in prod/charts/)
# Tune API, webhooks, etc. per docs: https://docs.kargo.io

# Pflichtfelder (keine Defaults): in prod/values.yaml unter kargo.api.adminAccount setzen:
#   passwordHash, tokenSigningKey (Generierung siehe Kargo-Docs / README)
# api.service.type: ClusterIP (default) or NodePort for external access
