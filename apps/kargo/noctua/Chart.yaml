apiVersion: v2
name: kargo-wrapper
description: Wrapper for Kargo Helm chart (OCI)
version: 0.1.0
dependencies:
  # Kargo OCI chart; dependency is vendored (charts/) for Argo CD
  - name: kargo
    version: "1.9.1"
    repository: oci://ghcr.io/akuity/kargo-charts
