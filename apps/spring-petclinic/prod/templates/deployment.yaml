apiVersion: apps/v1
kind: Deployment
metadata:
  name: spring-petclinic
  labels:
    app: spring-petclinic
spec:
  replicas: {{ .Values.replicas | default 1 }}
  selector:
    matchLabels:
      app: spring-petclinic
  template:
    metadata:
      labels:
        app: spring-petclinic
      annotations:
        # Instrumentation CR im Namespace otel-operator (cross-namespace)
        instrumentation.opentelemetry.io/inject-java: "otel-operator/java-instrumentation"
        # Service-Name für Mimir/Tempo/Grafana (Filter, Labels)
        resource.opentelemetry.io/service.name: "spring-petclinic"
    spec:
      containers:
        - name: petclinic
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          resources:
            requests:
              cpu: {{ .Values.resources.requests.cpu | default "100m" }}
              memory: {{ .Values.resources.requests.memory | default "256Mi" }}
            limits:
              memory: {{ .Values.resources.limits.memory | default "512Mi" }}
---
apiVersion: v1
kind: Service
metadata:
  name: spring-petclinic
  labels:
    app: spring-petclinic
spec:
  selector:
    app: spring-petclinic
  ports:
    - port: 8080
      targetPort: 8080
      name: http
