apiVersion: kargo.akuity.io/v1alpha1
kind: Stage
metadata:
  name: grafana-int
  namespace: grafana
spec:
  requestedFreight:
    - origin:
        kind: Warehouse
        name: grafana-warehouse
      sources:
        stages:
          - grafana-dev
  promotionTemplate:
    spec:
      vars:
        - name: gitRepo
          value: https://github.com/saadisfy/bwcloud-gitops.git
        - name: targetBranch
          value: main
      steps:
        - uses: git-clone
          config:
            repoURL: ${{ vars.gitRepo }}
            checkout:
              - branch: ${{ vars.targetBranch }}
                path: ./out
        - uses: copy
          as: copy-chart
          config:
            inPath: ./out/apps/grafana/dev/Chart.yaml
            outPath: ./out/apps/grafana/int/Chart.yaml
        - uses: copy
          as: copy-templates
          config:
            inPath: ./out/apps/grafana/dev/templates
            outPath: ./out/apps/grafana/int/templates
        - uses: git-commit
          config:
            path: ./out
            message: |
              chore(grafana): promote dev → int
        - uses: git-push
          config:
            path: ./out
            targetBranch: ${{ vars.targetBranch }}
